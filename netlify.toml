[build]
  base = "artifacts/peywend"
  command = "pnpm run build"
  publish = "dist"

[build.environment]
  PORT = "3000"
  BASE_PATH = "/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
