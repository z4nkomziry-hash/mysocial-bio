[build]
  base = "artifacts/peywend"
  command = "pnpm run build"
  publish = "dist/public"

[build.environment]
  PORT = "3000"
  BASE_PATH = "/"

[[redirects]]
  from = "/*"
  to = "/index.html"
  status = 200
